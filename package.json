{
  "name": "thai-flashcards",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thai Flashcards — aprendizaje de tailandés con SRS",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
